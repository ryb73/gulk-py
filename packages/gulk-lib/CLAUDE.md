## What this is

Gulk is a trick-taking card game. The gulk-lib package contains event and state definitions as well as game logic.

## Commands

Relevant commands:

- `make test` — run the test suite
- `make check` — run all static checks
- `make update-snapshots` — regenerate inline snapshots for tests
